#functions
#go home and ls 
function cl() {
    DIR="$*";
        # if no DIR given, go home
        if [ $# -lt 1 ]; then 
                DIR=$HOME;
    fi;
    builtin cd "${DIR}" && \
        ls-go -alkSn
}

#mkdir and change to it
function mcd () {
    mkdir -p $1
    cd $1
}
